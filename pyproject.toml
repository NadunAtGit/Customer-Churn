[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "retention-ai"
version = "0.1.0"
description = "Production-grade customer churn and retention intelligence platform"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]